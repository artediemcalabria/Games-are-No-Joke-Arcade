# Optional. Leave empty on Firebase Hosting: the app uses /api/coach.
VITE_AI_COACH_ENDPOINT=""

# Optional. Leave empty on Firebase Hosting: the app uses /api/prototype-image.
VITE_AI_IMAGE_ENDPOINT=""

# GEMINI_API_KEY belongs only in Firebase Functions secrets.
# Do not expose it in this frontend app, GitHub Pages, or .env.local.
